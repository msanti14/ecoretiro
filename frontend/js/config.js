/**
 * EcoRetiro — Production configuration
 *
 * Before uploading to GitHub Pages, replace the URL below with
 * the real backend URL (e.g. your ngrok HTTPS tunnel).
 *
 * Example:
 *   window.ECORETIRO_API_URL = 'https://abc123.ngrok-free.app';
 *
 * Leave empty ('') to fall back to http://127.0.0.1:8000 (local dev).
 */
window.ECORETIRO_API_URL = '';
